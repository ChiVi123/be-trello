export const DEFAULT_PAGE = "1";
export const DEFAULT_ITEMS_PER_PAGE = "12";

export enum BOARD_TYPES {
    PUBLIC = "public",
    PRIVATE = "private",
}
export enum INVITATION_TYPES {
    BOARD = "board",
}
export enum BOARD_INVITATION_STATUS {
    PENDING = "pending",
    ACCEPTED = "accepted",
    REJECTED = "rejected",
}
